export class Design {

    fontSize: number;
    fontWeight: number;
    lineHeight: number;
 constructor( 
    fontSize: number,
    fontWeight: number,
    lineHeight: number

 ){
     this.fontSize = fontSize;
     this.fontWeight = fontWeight;
     this.lineHeight = lineHeight;

 }
}